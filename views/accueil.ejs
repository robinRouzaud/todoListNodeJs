<!DOCTYPE html>
<html>
    <head>
        <title>Page d'accueil de la ToDo list</title>
    </head>
    <body>
        <h2>On va essayer de faire une ToDo list dans cet exercice en suivant ce <a href="http://192.168.1.17:8080/todolist">lien</a></h2>
        <div name="login">
            <form method="POST" action="/load">
                <input type="text" name="username" value="emailDAO10@test.com">
                <input type="text" name="password" value="password">
                <input type="submit" value="valider">
            </form>
        </div>
        <% if(wrongPassword === true && userFound === true) { %>
            <script>
                alert('Le mot de passe entré est erroné.');
            </script>
        <% } %>
        <% if(wrongPassword === false && userFound === false) { %>
            <script>
                alert('L\'utilisateur n\'a pas été trouvé.');
            </script>
        <% } %>
    </body>
</html>